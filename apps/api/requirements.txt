fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
httpx>=0.27,<1.0
# Deriv's market data is WebSocket-only. It already arrives with
# uvicorn[standard], but this module imports it directly, and a
# transitive dependency is one `pip install --no-deps` away from
# disappearing without anything saying why.
websockets>=13.0,<16.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
apscheduler>=3.10,<4.0
supabase>=2.20,<3.0
python-dotenv>=1.0,<2.0
